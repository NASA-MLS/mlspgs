include ../Makefile.dep

# "$Id$"

# ========================================================================
ALLOBJS = $(OBJS)

PVM_PATH = ${PVM_ROOT}/lib/${PVM_ARCH}

libl3_sources = ../../../$(init_m_dir)/

libl3_f90 := $(wildcard $(libl3_sources)/*.f90)

libl3_objs := $(wildcard $(itmdir)/*.o)

l3_driver = MLSL3.o

# ========================================================================
libl3.a: $(libl3_objs)
	ar ru $@ $?
	ar d $@ $(itmdir)/$(l3_driver)

test: $(ALLOBJS) $(PVM_OBJS) libl3.a $(libdir)/libmls.a
	case "$(PVM_ROOT)" in \
		"") \
		echo "Building test without PVM support" ; \
		$(FC) $(LDOPTS) -o test $(ALLOBJS) \
                      -L./ -ll3 \
                      -L${libdir} -lmls \
		-L${PGSTK} -lPGSTK \
        -L${HDFEOS} -lhdfeos \
		-L${HDF} -lmfhdf \
        -ldf \
        -lz \
        -ljpeg \
		-L${GCTP} -lGctp \
        $(l_specials) ; \
		;; \
		*) \
		echo "Building test with PVM support" ; \
		$(FC) $(LDOPTS) -o test $(ALLOBJS) $(PVM_OBJS)  \
                      -L./ -ll3 \
                      -L${libdir} -lmls \
		-L${PGSTK} -lPGSTK \
        -L${HDFEOS} -lhdfeos \
		-L${HDF} -lmfhdf \
        -ldf \
        -lz \
        -ljpeg \
		-L${GCTP} -lGctp \
		-L${PVM_PATH} -lfpvm3 -lgpvm3 -lpvm3 \
        $(l_specials) ; \
		;; \
	esac 
# ========================================================================

clean:
	rm -f *.o *.mod test

tar:
	rm -f libtest.tgz
	tar hcvzf libtest.tgz * --exclude \*.o --exclude \*.mod --exclude test

# ========================================================================
# $Log$
# Revision 1.1  2001/02/01 23:12:32  pwagner
# First commit
#
#
