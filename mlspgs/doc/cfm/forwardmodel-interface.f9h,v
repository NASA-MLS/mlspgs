head	1.4;
access;
symbols
	v5-02-NRT-19:1.4
	v6-00:1.4
	v5-02-NRT-18:1.4
	v5-02:1.4
	v5-01-NRT-17:1.4
	v5-01-NRT-16:1.4
	v5-01-NRT-15:1.4
	v5-01-NRT-14:1.4
	neuralnetworks-1-0:1.4.0.12
	cfm-single-freq-0-1:1.4.0.10
	v5-01:1.4
	v5-00:1.4
	v4-23-TA133:1.4.0.8
	mus-emls-1-70:1.4.0.6
	rel-1-0-englocks-work:1.4.0.4
	VUMLS1-00:1.4
	VPL1-00:1.4
	V4-22-NRT-08:1.4
	VAM1-00:1.4
	V4-21:1.4.0.2
	V4-13:1.4
	V4-12:1.4
	V4-11:1.4
	V4-10:1.4
	V3-43:1.1
	M4-00:1.4
	V3-41:1.1
	V3-40-PlusGM57:1.1.0.2
	V3-33:1.3
	V3-31:1.3
	V3-30-NRT-05:1.3
	cfm-01-00:1.3
	V3-30:1.1
	V3-20:1.1;
locks; strict;
comment	@# @;


1.4
date	2011.12.16.01.19.40;	author honghanh;	state Exp;
branches;
next	1.3;

1.3
date	2010.06.16.18.37.41;	author honghanh;	state Exp;
branches;
next	1.2;

1.2
date	2010.05.24.18.35.57;	author honghanh;	state Exp;
branches;
next	1.1;

1.1
date	2009.12.21.23.28.26;	author honghanh;	state Exp;
branches;
next	;


desc
@@


1.4
log
@Updated documentation for newer API centered on the use of agile vectors,
and removed the concept of vector template and chunk from public API.
@
text
@! Compute radiances based on atmospheric state information provided
! by fwdModelIn and fwdModelExtra.
subroutine ForwardModelWMaf (RequestedMAF, Config, FwdModelIn, FwdModelExtra, &
                             FwdModelOut, Jacobian )
    ! The 0-based index of the instance (maf or profile) stored in
    ! fwdModelIn to run the forward model over.
    ! If there is only 1 instance, then this field should be 0.
    ! If there is N instance, then the appropriate values for this
    ! field is 0 to N-1.
    integer, intent(in) :: RequestedMAF
    ! Configuration information for the forward model
    type(ForwardModelConfig_T), dimension(:), intent(inout) :: CONFIG
    ! Atmospheric input
    type(vector_T), intent(in) ::  FWDMODELIN, FwdModelExtra
    ! This is the output vector where radiances are stored
    type(vector_T), intent(inout) :: FWDMODELOUT  ! Radiances, etc.
    ! The matrix equivalent of the forward model with the specific
    ! setting in the config object.
    type(matrix_T), intent(inout), optional :: JACOBIAN
end subroutine
@


1.3
log
@Updated interface for forward model
@
text
@d1 19
a19 24
!Relates an atmospheric state, representing by a state vector 
!and an optional extra state vector, to MLS radiances, also 
!representing by a vector.
subroutine ForwardModel (chunk,  fmConfig, stateVector, stateVectorExtra, &
   radiances, Jacobian)
   !Object carrying the MAFs to be computed over
   type(MLSChunk_T), intent(in) :: chunk

   !An array of objects containing the settings for the forward model.
   type(ForwardModelConfig_T), dimension(:), intent(inout) :: fmConfig
   
   !A vector describing an atmospheric state.
   type(vector_T), intent(in) ::  stateVector

   !A vector describing other properties of the atmosphere
   ! that are not described by stateVector
   type(Vector_T), intent(in), optional :: stateVectorExtra

   !A vector to store the output of the forward model.
   type(Vector_T), intent(inout) :: radiances

   !The matrix equivalent of the forward model with the
   !specific setting in fmConfig.
   type(Matrix_T), intent(inout), optional :: JACOBIAN
@


1.2
log
@v0.5 updated document
@
text
@d9 2
a10 2
   !An object containing the settings for the forward model.
   type(ForwardModelConfig_T), intent(inout) :: fmConfig
@


1.1
log
@Replace description with code snippet in the procedure reference portion
@
text
@d4 5
a8 2
subroutine ForwardModel ( fmConfig, stateVector, stateVectorExtra, &
   radiances, fmStat, Jacobian)
a21 3
   !This object is used internally.
   type(ForwardModelStatus_t), intent(inout) :: fmStat

@

