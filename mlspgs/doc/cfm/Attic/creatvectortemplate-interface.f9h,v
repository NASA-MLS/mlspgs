head	1.3;
access;
symbols
	V3-43:1.1
	V3-41:1.1
	V3-40-PlusGM57:1.1.0.2
	V3-33:1.2
	V3-31:1.2
	V3-30-NRT-05:1.2
	cfm-01-00:1.2
	V3-30:1.1
	V3-20:1.1;
locks; strict;
comment	@# @;


1.3
date	2011.12.16.01.19.40;	author honghanh;	state dead;
branches;
next	1.2;

1.2
date	2010.03.26.15.33.03;	author honghanh;	state Exp;
branches;
next	1.1;

1.1
date	2009.12.21.23.28.26;	author honghanh;	state Exp;
branches;
next	;


desc
@@


1.3
log
@Updated documentation for newer API centered on the use of agile vectors,
and removed the concept of vector template and chunk from public API.
@
text
@! Create a VectorTemplate_T, which references one or many quantities.
! To ensure correctness, the same qtyTemplateDB, must be passed to
! CreateVector function in conjunction with the returned VectorTemplate_T.
type(VectorTemplate_T) function CreateVectorTemplate (qtyTemplateDB, selectedQty) &
   result (vecTemplate)
   ! a list of the array indices of the chosen quantity templates.
   integer, intent(in) :: selectedQty(:)
   ! a 1-based, 1-dimensional array of all QuantityTemplate_T objects
   type(QuantityTemplate_T), intent(in) :: qtyTemplateDB(:)
end function
@


1.2
log
@Update for v0.1 document
Add subroutines creating grids
Add ExplicitFillVectorQuantity subroutine
Add FGrid_T data structure
Modify mockup.f90
Removing creating quantity template example because it can be found in the mockup
@
text
@@


1.1
log
@Replace description with code snippet in the procedure reference portion
@
text
@d1 9
a9 11
!Creates a vector template.
type(VectorTemplate_T) function CreateVectorTemplate (qtyTemplateDB, selected)
   !An array of QuantityTemplate\_T objects describing all available 
   !quantities.
   type(QuantityTemplate_T), dimension(:), pointer :: qtyTemplateDB

   !An array of logicals of the same size as qtyTemplateDB,
   !whose elements are true where the corresponding
   !qtyTemplateDB's elements are used by the vector of
   !this vector template.
   logical, dimension(:), pointer :: selected
@

