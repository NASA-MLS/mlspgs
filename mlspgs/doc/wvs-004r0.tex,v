head	1.1;
access;
symbols
	v5-02-NRT-19:1.1
	v6-00:1.1
	v5-02-NRT-18:1.1
	v5-02:1.1
	v5-01-NRT-17:1.1
	v5-01-NRT-16:1.1
	v5-01-NRT-15:1.1
	v5-01-NRT-14:1.1
	neuralnetworks-1-0:1.1.0.14
	cfm-single-freq-0-1:1.1.0.12
	v5-01:1.1
	v5-00:1.1
	v4-23-TA133:1.1.0.10
	mus-emls-1-70:1.1.0.8
	rel-1-0-englocks-work:1.1.0.6
	VUMLS1-00:1.1
	VPL1-00:1.1
	V4-22-NRT-08:1.1
	VAM1-00:1.1
	V4-21:1.1.0.4
	V4-13:1.1
	V4-12:1.1
	V4-11:1.1
	V4-10:1.1
	V3-43:1.1
	M4-00:1.1
	V3-41:1.1
	V3-40-PlusGM57:1.1.0.2
	V3-33:1.1
	V3-31:1.1
	V3-30-NRT-05:1.1
	cfm-01-00:1.1
	V3-30:1.1
	V3-20:1.1
	V3-10:1.1;
locks; strict;
comment	@% @;


1.1
date	2008.06.11.20.14.50;	author vsnyder;	state Exp;
branches;
next	;


desc
@@


1.1
log
@Initial commit
@
text
@\documentclass[11pt]{article}
\textwidth 6.25in
\oddsidemargin -0.25in
%\evensidemargin -0.5in
\topmargin -0.5in
\textheight 9.00in

\begin{document}

%\tracingcommands=1
\newlength{\hW} % heading box width
%\settowidth{\hW}{\bf wvs-004}
\settowidth{\hW}{Page \pageref{lastpage}\ of \pageref{lastpage}}
\makeatletter
\def\@@biblabel#1{#1.}
\newcommand{\ps@@twolines}{%
  \renewcommand{\@@oddhead}{%
    27 April 2000\hfill\parbox[t]{\hW}{{\bf wvs-004}\newline
                          Page \thepage\ of \pageref{lastpage}}}%
\renewcommand{\@@evenhead}{}%
\renewcommand{\@@oddfoot}{}%
\renewcommand{\@@evenfoot}{}%
}%
\makeatother
\pagestyle{twolines}

\vspace{-10pt}
\begin{tabbing}
\phantom{References: }\= \\
To: \>Dave, Nathaniel\\
Subject: \>Grammar for the L2CF\\
From: \>Van Snyder\\
\end{tabbing}

\parindent 0pt \parskip 3pt
\vspace{-20pt}

\section{Notation}

The general form is ``LHS $\rightarrow$ list-of-things''

Things in quotes must appear literally, or are the names of categories
of things deduced by the Lexer.

\begin{itemize}
\item[$\rightarrow$] is pronounced ``is defined to be'' or ``produces''.
\item[*] means ``zero or more of the preceeding single item or parenthesized group''
\item[+] means ``one or more of the preceeding single item or parenthesized group''
\item[?] means ``zero or one of the preceeding single item or parenthesized group''
\item[$\mid$] means ``or''
\end{itemize}
Parentheses are used for grouping.

Low-level issues, such as comments and statement continuation, are
not described here.

\section{The grammar}\label{gram}

\begin{tabular}{|l|l|}
     \hline
{\bf Productions} & {\bf Selection Sets} \\
     \hline
     \hline
cf $\rightarrow$ one\_cf + `EOF'                  & `begin' \\
     \hline
one\_cf $\rightarrow$ `begin' `name' spec + `end' `name'  & `begin' \\
     \hline
spec $\rightarrow$ `name' spec\_rest `EOS'        & `name' \\
     \hline
spec\_rest $\rightarrow$                          & `EOS' \\
     \phantom{spec\_rest}
          $\rightarrow$ `=' expr +                & `=' \\
     \phantom{spec\_rest}
          $\rightarrow$ ( `,' spec\_list ) +      & `,' \\
     \hline
spec\_list $\rightarrow$ `name' ( `=' expr + ) ?  & `name' \\
     \phantom{spec\_list}
          $\rightarrow$ `/' `name'                & `/' \\
     \hline
expr $\rightarrow$ `string' +                     & `string' \\
     \phantom{expr}
     $\rightarrow$ value                          & \{ `name', `number', `(' \} \\
     \hline
value $\rightarrow$ limit ( `..' limit ) ?        & \{ `name', `number', `(' \} \\
     \hline
limit $\rightarrow$ term ( ( `+' $\mid$ `-' ) term ) *  & same \\
     \hline
term $\rightarrow$ primary ( ( `*' $\mid$ `/' ) primary ) *  & same \\
     \hline
primary $\rightarrow$ `name' ( `.' `name' ) ?     & `name' \\
     \phantom{primary}
        $\rightarrow$ `number' unit ?             & `number' \\
     \phantom{primary}
        $\rightarrow$ `(' value `)'               & `(' \\
     \hline
\end{tabular}

Within the grammar, a symbol that appears immediately before the
$\rightarrow$ symbol is called ``nonterminal.''  ``Nonterminal'' means
that if the grammar is being used to produce a sentence in the language
it denotes, a nonterminal symbol can be replaced by one of its
corresponding right-hand-sides.

Symbols within single quotes are called ``terminal'' because there
is no rule to replace them.  They are also called ``tokens.''  The
grouping of characters into tokens is deduced by a procedure called the
``Lexer.''

\section{Organization of the Parser}

The parser consists of a collection of potentially recursive procedures.

There is one procedure for each left-hand-side in the grammar of section
\ref{gram}.

Within each procedure, there is a selection process:  If the next token
is within the selection set for a production with a left-hand-side symbol
that corresponds to that procedure, that production is ``executed.''  If
the next token is not within the selection set for any production with a
left-hand-side symbol that corresponds to that procedure, an error is
announced.

``Executing'' a production's right-hand-side consists of executing the
procedures that correspond to names encountered in the right-hand-side,
or ``consuming'' the next token if the next entity in the right-hand-side
is not the name on the left-hand-side of any production, in the order
these entities appear.

``Consuming'' a token consists of comparing it to the expected token,
fetching a new token if they are equal, and announcing an error otherwise.

In the abbreviated form of the grammar in section \ref{gram}, it will be
necessary also to have a selection process for optional or repeated parts
within the right-hand-side.  These selection sets are tabulated in part
\ref{extend}.  The correspondence between selection sets in part
\ref{extend} and the selection sets for optional or repeated parts within
the right-hand-sides of the grammar in section \ref{gram} should be obvious.

\section{Extended form of the grammar}\label{extend}

\begin{tabular}{|l|l|}
     \hline
{\bf Productions} & {\bf Selection Sets} \\
     \hline
     \hline
cf $\rightarrow$ one\_cf more\_cfs `EOF'         & `begin' \\
     \hline
more\_cfs $\rightarrow$                          & `EOF' \\
     \phantom{more\_cfs}
         $\rightarrow$ one\_cf                   & `begin' \\
     \hline
one\_cf $\rightarrow$ `begin' `name' spec specs `end' `name'  & `begin' \\
     \hline
specs $\rightarrow$                              & `end' \\
     \phantom{specs}
      $\rightarrow$ spec specs                   & `name' \\
     \hline
spec $\rightarrow$ `name' spec\_rest `EOS'       & `name' \\
     \hline
spec\_rest $\rightarrow$                         & `EOS' \\
     \phantom{spec\_rest}
          $\rightarrow$ `=' expr +               & `=' \\
     \phantom{spec\_rest}
          $\rightarrow$ `,' spec\_list more\_specs     & `,' \\
     \hline
more\_specs $\rightarrow$                        & `EOS' \\
     \phantom{more\_specs}
            $\rightarrow$ `,' spec\_list more\_specs     & `,' \\
     \hline
spec\_list $\rightarrow$ `name' more\_spec\_list & `name' \\
     \phantom{spec\_list}
          $\rightarrow$ `/' `name'               & `/' \\
     \hline
more\_spec\_list $\rightarrow$                   & \{ `,' `EOS' \} \\
     \phantom{more\_spec\_list}
                 $\rightarrow$ `=' expr exprs    & `=' \\
     \hline
exprs $\rightarrow$                              & \{ `,' `EOS' \} \\
     \phantom{exprs}
      $\rightarrow$ expr exprs                   & \{ `name' `number'
                                                      `string' `(' \} \\
     \hline
expr $\rightarrow$ `string' strings              & `string' \\
     \phantom{expr}
     $\rightarrow$ value                         & \{ `name' `number' `(' \} \\
     \hline
strings $\rightarrow$                            & \{ `,' `EOS' \} \\
     \phantom{strings}
        $\rightarrow$ `string'                   & `string' \\
     \hline
value $\rightarrow$ limit range                  & \{ `name', `number', `(' \} \\
     \hline
range $\rightarrow$                              & \{ `,' `EOS' ')' \} \\
     \phantom{range}
      $\rightarrow$ `..' limit                   & \{ `name', `number', `(' \} \\
     \hline
limit $\rightarrow$ term term\_list              & \{ `name', `number', `(' \} \\
     \hline
term\_list $\rightarrow$                         & \{ `..' `,' `EOS' ')' \} \\
     \phantom{term\_list}
           $\rightarrow$ ( `+' $\mid$ `-' ) term term\_list & \{ `+' `-' \} \\
     \hline
term $\rightarrow$ primary primary\_list         & \{ `name', `number', `(' \} \\
     \hline
primary\_list $\rightarrow$                      & \{ `+' `-' `..' `,'
                                                      `EOS' ')' \}\\
     \phantom{primary\_list}
              $\rightarrow$ ( `*' $\mid$ `/' ) primary & \{ `*' `/' \} \\
     \hline
primary $\rightarrow$ `name' field               & `name' \\
     \phantom{primary}
        $\rightarrow$ `number' unit ?            & `number' \\
     \phantom{primary}
        $\rightarrow$ `(' expr `)'               & `(' \\
     \hline
field $\rightarrow$                              & \{  `*' `/' `+' `-' `..'
                                                       `,' `EOS' ')'\} \\
     \phantom{field}
      $\rightarrow$ `.' `name'                   & `.' \\
     \hline
\end{tabular}

\label{lastpage}
\end{document}
% $Id: $
@
