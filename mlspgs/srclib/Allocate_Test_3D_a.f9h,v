head	1.7;
access;
symbols
	v5-02-NRT-19:1.7
	v6-00:1.7
	v5-02-NRT-18:1.7
	v5-02:1.7
	v5-01-NRT-17:1.7
	v5-01-NRT-16:1.7
	v5-01-NRT-15:1.7
	v5-01-NRT-14:1.7
	neuralnetworks-1-0:1.7.0.6
	cfm-single-freq-0-1:1.7.0.4
	v5-01:1.7
	v5-00:1.7
	v4-23-TA133:1.7.0.2
	mus-emls-1-70:1.6.0.2
	rel-1-0-englocks-work:1.5.0.2
	VUMLS1-00:1.5
	VPL1-00:1.5
	V4-22-NRT-08:1.5
	VAM1-00:1.5
	V4-21:1.3.0.2
	V4-13:1.3
	V4-12:1.2
	V4-11:1.2
	V4-10:1.2
	V3-43:1.1
	M4-00:1.1
	V3-41:1.1
	V3-40-PlusGM57:1.1.0.2
	V3-33:1.1
	V3-31:1.1
	V3-30-NRT-05:1.1
	cfm-01-00:1.1
	V3-30:1.1
	V3-20:1.1
	V3-10:1.1;
locks; strict;
comment	@# @;


1.7
date	2018.10.12.20.08.15;	author pwagner;	state Exp;
branches;
next	1.6;

1.6
date	2018.04.19.00.43.00;	author vsnyder;	state Exp;
branches;
next	1.5;

1.5
date	2015.06.04.01.54.53;	author vsnyder;	state Exp;
branches;
next	1.4;

1.4
date	2015.03.28.00.53.02;	author vsnyder;	state Exp;
branches;
next	1.3;

1.3
date	2014.09.04.23.50.14;	author vsnyder;	state Exp;
branches;
next	1.2;

1.2
date	2013.06.12.02.16.51;	author vsnyder;	state Exp;
branches;
next	1.1;

1.1
date	2008.05.21.21.48.23;	author vsnyder;	state Exp;
branches;
next	;


desc
@@


1.7
log
@Even if didnt need to reallocate, respect fill option
@
text
@! Copyright 2005, by the California Institute of Technology. ALL
! RIGHTS RESERVED. United States Government Sponsorship acknowledged. Any
! commercial use must be negotiated with the Office of Technology Transfer
! at the California Institute of Technology.

! This software may be subject to U.S. export control laws. By accepting this
! software, the user agrees to comply with all applicable U.S. export laws and
! regulations. User has the responsibility to obtain export licenses, or other
! export authority as may be required before exporting such information to
! foreign countries or providing access to foreign persons.

!------------------------------- RCS Ident Info ------------------------------
! $RCSfile: Allocate_Test_3D_a.f9h,v $
!-----------------------------------------------------------------------------

! Probably only useful within Allocate_Deallocate.f90

    integer(c_intptr_t) :: Addr
    logical :: DoAlloc
    integer :: MyLow(3), Status
    character(127) :: ERMSG
    status = 0 ! In case we don't need to allocate
    myLow = 1
    if ( present(low) ) myLow = low
    doAlloc = .not. associated(to_allocate)
    if ( .not. doAlloc ) doAlloc = any(shape(to_allocate) /= dim - myLow + 1)
    if ( doAlloc ) then
      call deallocate_Test ( To_Allocate, ItsName, ModuleName )
      allocate ( To_Allocate(myLow(1):dim(1),myLow(2):dim(2),myLow(3):dim(3)), &
        & STAT=status, ERRMSG=ermsg )
      addr = 0
      if ( status == 0 ) then
        if ( size(to_allocate) > 0 ) &
          & addr = transfer ( c_loc(to_allocate(myLow(1),myLow(2),myLow(3))), addr )
      end if
      call test_allocate ( status, moduleName, ItsName, myLow, dim, &
        & bytes(to_allocate), ermsg, addr )
      if ( mod(trackallocates,2) > 0 ) &
        & call trackAllocate ( to_allocate, itsName, moduleName )
    else
      to_allocate(myLow(1):,myLow(2):,myLow(3):) => to_allocate
    end if
    if ( status == 0 ) then
      if ( present(fill) ) then
        to_allocate = fill
      else if ( clearOnAllocate ) then
        to_allocate = default
      end if
    end if

! $Log: Allocate_Test_3D_a.f9h,v $
! Revision 1.6  2018/04/19 00:43:00  vsnyder
! Turn on address computation for tracking
!
! Revision 1.5  2015/06/04 01:54:53  vsnyder
! Don't reallocate if bounds are the same
!
! Revision 1.4  2015/03/28 00:53:02  vsnyder
! Stuff to trace allocate/deallocate addresses -- mostly commented out
! because NAG build 1017 doesn't yet allow arrays as arguments to C_LOC.
!
! Revision 1.3  2014/09/04 23:50:14  vsnyder
! Add output of error message from allocation failure
!
! Revision 1.2  2013/06/12 02:16:51  vsnyder
! Add and use BYTES and BYTE_SIZE
!
! Revision 1.1  2008/05/21 21:48:23  vsnyder
! Initial commit
!
@


1.6
log
@Turn on address computation for tracking
@
text
@d22 1
a32 5
        if ( present(fill) ) then
          to_allocate = fill
        else if ( clearOnAllocate ) then
          to_allocate = default
        end if
d43 7
d52 3
@


1.5
log
@Don't reallocate if bounds are the same
@
text
@d29 1
a29 1
        & stat=status, ERRMSG=ERMSG )
d37 2
a38 2
!         if ( size(to_allocate) > 0 ) &
!           & addr = transfer ( c_loc(to_allocate(myLow(1),myLow(2),myLow(3))), addr )
d41 1
a41 1
        & bytes(to_allocate), ERMSG, addr )
d49 3
@


1.4
log
@Stuff to trace allocate/deallocate addresses -- mostly commented out
because NAG build 1017 doesn't yet allow arrays as arguments to C_LOC.
@
text
@d19 2
a20 1
    integer :: MyLow(3), STATUS
a21 1
    call deallocate_Test ( To_Allocate, ItsName, ModuleName )
d24 15
a38 8
    addr = 0
    allocate ( To_Allocate(myLow(1):dim(1),myLow(2):dim(2),myLow(3):dim(3)), &
      & stat=status, ERRMSG=ERMSG )
    if ( status == 0 ) then
      if ( present(fill) ) then
        to_allocate = fill
      else if ( clearOnAllocate ) then
        to_allocate = default
d40 6
a45 2
!       if ( size(to_allocate) > 0 ) &
!         & addr = transfer ( c_loc(to_allocate(myLow(1),myLow(2),myLow(3))), addr )
a46 4
    call test_allocate ( status, moduleName, ItsName, myLow, dim, &
      & bytes(to_allocate), ERMSG, addr )
    if ( mod(trackallocates,2) > 0 ) &
      & call trackAllocate ( to_allocate, itsName, moduleName )
d49 4
@


1.3
log
@Add output of error message from allocation failure
@
text
@d18 1
d24 1
a26 2
    call test_allocate ( status, moduleName, ItsName, myLow, dim, &
      & bytes(to_allocate), ERMSG )
d33 2
d36 4
a39 1
    if ( trackallocates > 0 ) call trackAllocate ( to_allocate, itsName, moduleName )
d42 3
@


1.2
log
@Add and use BYTES and BYTE_SIZE
@
text
@d19 1
d24 3
a26 2
      & stat=status )
    call test_allocate ( status, moduleName, ItsName, myLow, dim, bytes(to_allocate) )
d37 3
@


1.1
log
@Initial commit
@
text
@d13 1
a13 1
! $RCSfile: Allocate_Test_2D.f9h,v $
d24 1
a24 1
    call test_allocate ( status, moduleName, ItsName, myLow, dim, eSize )
d34 4
a37 1
! $Log: Allocate_Test_2D.f9h,v $
@

