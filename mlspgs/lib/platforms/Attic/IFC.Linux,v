head	1.4;
access;
symbols
	V2-21:1.1
	V2-20:1.1
	V2-11:1.1
	V2-10:1.1
	V2-00:1.1
	V1-51:1.1
	V1-50:1.1
	V1-45:1.1
	V1-44:1.1
	V1-43:1.1
	V1-42:1.1
	V1-41:1.1
	V1-32:1.1
	V1-40:1.1
	V1-31:1.1
	V1-30:1.1;
locks; strict;
comment	@# @;


1.4
date	2007.06.07.19.55.30;	author pwagner;	state dead;
branches;
next	1.3;

1.3
date	2007.05.17.16.06.24;	author pwagner;	state Exp;
branches;
next	1.2;

1.2
date	2007.04.14.00.39.59;	author vsnyder;	state Exp;
branches;
next	1.1;

1.1
date	2003.04.01.23.20.53;	author pwagner;	state Exp;
branches;
next	;


desc
@@


1.4
log
@compiler-specific pieces removed from platforms directory
@
text
@#Makefile.mac -- a file full of macros to be included by a Makefile
#to compile a Fortran 9x program or library

# "$Id: IFC.Linux,v 1.3 2007/05/17 16:06:24 pwagner Exp $"

SHELL = /bin/sh

# Path to source files:
S = ..
# Path to root directory of l2, lib, etc.:
CONFDIR=../..

UTILDIR=../../util

# reecho.sh can filter out globbed file expansions that fail
# e.g., '*.f90 *.f' -> '*.f90" if there aren't any .f files
# However we will be using it just to create lapack_linker_line 
# where LIB_LAPACK may contain multiple libraries
REECHO=$(UTILDIR)/reecho.sh

TESTSDIR=../../tests

# include files %.f9h shared by l2, lib are in $(srclib)
srclib = $(CONFDIR)/srclib

# .mod files needed by lib are in $(blaslib)
blaslib = $(CONFDIR)/blas/$(MLSCONFG)

# This is the machines directory--its files are compiled directly into libdir
dir1 =

# This is the srclib directory--its files are compiled directly into libdir
dir2 =

# This is the blas directory
dir3 = $(blaslib)/

# Do the load options include garbage collection?
# The answer  will determine what we do to the machine.f90 file from
# (  (not applicable yet for this compiler/platform))
MACH_DIR=../machines/$(MLSCONFG)
GC_IN_LDOPTS := $(shell echo ${LDOPTS} | grep '\-gc')
ifeq ($(GC_IN_LDOPTS),)
BAD_MATCH=-gc
else
BAD_MATCH=no -gc
endif

# Set paths for finding source files (different from .o and .mod)

vpath %.mod ./
vpath %.o ./
vpath %.f90 ../ $(MACH_DIR)
vpath %.F90 ../ $(MACH_DIR)
vpath %.f9h ../ $(srclib)
vpath %.f ../ $(PGSINC)
vpath %.c ../

FC = ifort

# Currently, unknown if DUSTY needs to be different yet
DUSTY = $(FOPTS)

# Use the lib directory, too
libdir = $(CONFDIR)/lib/$(MLSCONFG)

# All the paths to supply .mod and INCLUDE files used in compiling %.f90
# Precede each path by "-I"
PATHS_PREFXD := $(shell ${REECHO} -d -prefixn=-I $(UNPREFXDPTHS)) 

INC_PATHS = $(EXTRA_PATHS) $(PATHS_PREFXD) \
 -I$(PGSINC) -I$(srclib) -I$S -I$(blaslib)

# Define some suffix rules

.SUFFIXES:
.SUFFIXES: .o .dbg.o .f90 .f .c
.f90.o:
	$(FC) -c $(FOPTS) $(INC_PATHS) $< $(FAFTER)

.f90.dbg.o:
	$(FC) -c $(FOPTS) $(INC_PATHS) $< -o $*.dbg.o $(FAFTER)

.f.o:
	$(FC) -c $(FOPTS) $(INC_PATHS) $< $(FAFTER)

.c.o:
	$(CC) -c ${INC_PATHS} -DNAGf90Fortran $<

# $Log: IFC.Linux,v $
# Revision 1.3  2007/05/17 16:06:24  pwagner
# Intel Fortran now named ifort
#
# Revision 1.2  2007/04/14 00:39:59  vsnyder
# Added vpath for F90
#
# Revision 1.1  2003/04/01 23:20:53  pwagner
# First commit
#
@


1.3
log
@Intel Fortran now named ifort
@
text
@d4 1
a4 1
# "$Id: IFC.Linux,v 1.2 2007/04/14 00:39:59 vsnyder Exp $"
d91 3
@


1.2
log
@Added vpath for F90
@
text
@d4 1
a4 1
# "$Id: IFC.Linux,v 1.1 2003/04/01 23:20:53 pwagner Exp $"
d59 1
a59 1
FC = ifc
d91 3
@


1.1
log
@First commit
@
text
@d4 1
a4 1
# "$Id: Abs.Linux,v 1.14 2002/07/11 22:00:17 pwagner Exp $"
d51 1
d53 2
a54 1
vpath %.f90 ../ ../machines/$(MLSCONFG)
d90 4
a93 1
# $Log: Abs.Linux,v $
@

