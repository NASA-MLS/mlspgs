; --------------------------------------------- ChunkDivide ----
;
; This l2cf fragment gives a single chunk of length 1 MAF with
; no overlaps.
; It is sized appropriately for MLS UARS
begin ChunkDivide
  ChunkDivide, method=fixed, !ifdef(flagSkipL1BCheck,{/skipL1BCheck,})$
    maxLength=1 MAFs, overlap=0 MAFs 
end ChunkDivide
