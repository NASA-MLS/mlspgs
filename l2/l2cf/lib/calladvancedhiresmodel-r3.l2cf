;$Id$
;; calladvancedhiresmodel-r3
;; Take the current 'state' and produces a full forward
;; model estimate of bands 8, 33

;; Setup the vectors
begin Fill
  Transfer, source=state, destination=r3X_HR
end Fill

;; Do a sids calculation
begin Retrieve
  !ifdef(flagSkipAdvancedSids,{},{
  sids, fwdModelIn=state, fwdModelOut=[r3Y_HR], $
        forwardModel=[r3SetupModel_HR]})
end Retrieve

;$Log$
